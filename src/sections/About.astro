---
import Icon from '@components/Icon.astro';
import { imageSrc } from '@lib/sanity';

type Props = { block: any };
const { block } = Astro.props;
const imgUrl = block?.image ? imageSrc(block.image, 1200) : '';
const highlights = block?.highlights ?? [];
---

<section id="about" class="section relative bg-navy-50/40">
  <div class="container-x grid gap-16 lg:grid-cols-2 lg:items-center">
    <div class="relative reveal">
      <div class="aspect-[4/5] overflow-hidden rounded-2xl shadow-card">
        <img
          src={imgUrl}
          alt={block?.image?.alt || 'LNH & Partner'}
          width=1300
          height=1682
          class="h-full w-full object-cover transition-transform duration-700 hover:scale-105"
          loading="lazy"
        />
      </div>

      {block?.badgeValue && (
        <div class="absolute -bottom-6 -left-6 hidden rounded-xl border border-gold-200 bg-white p-6 shadow-card md:block">
          <p class="font-display text-3xl font-bold text-navy-900">{block.badgeValue}</p>
          {block?.badgeLabel && (
            <p class="text-xs font-semibold uppercase tracking-[0.15em] text-steel-500">
              {block.badgeLabel}
            </p>
          )}
        </div>
      )}

      <div class="absolute -right-4 -top-4 -z-10 h-32 w-32 rounded-full bg-gold-500/10 blur-2xl"></div>
    </div>

    <div class="reveal">
      {block?.eyebrow && <p class="eyebrow">{block.eyebrow}</p>}
      <h2 class="heading-section mt-4 text-balance">
        {block?.headingStart}
        {block?.headingAccent && <> <span class="text-gold-500">{block.headingAccent}</span></>}
      </h2>

      {block?.lead && <p class="lead mt-6">{block.lead}</p>}
      {block?.body && <p class="mt-5 text-base leading-relaxed text-steel-600">{block.body}</p>}

      {highlights.length > 0 && (
        <ul class="mt-8 grid gap-3 sm:grid-cols-2">
          {highlights.map((point: any) => (
            <li class="flex items-center gap-3 text-sm font-medium">
              <span class="inline-flex h-6 w-6 items-center justify-center rounded-full bg-gold-500/15 text-gold-600">
                <Icon name="check" class="h-3.5 w-3.5" />
              </span>
              {point.text}
            </li>
          ))}
        </ul>
      )}
    </div>
  </div>
</section>
